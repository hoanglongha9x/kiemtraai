rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // V3.0 MVP: upload media nên đi qua signed upload/server validation trước.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
